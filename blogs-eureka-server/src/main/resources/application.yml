server:
  port: 8080

spring:
  application:
    name: blogs-eureka-server
  main:
    allow-bean-definition-overriding: true
eureka:
  client:
    fetch-registry: false
    register-with-eureka: false
    service-url:
      defaultZone: http://localhost:8080/eureka
  instance:
    appname: eureka
    hostname: localhost




